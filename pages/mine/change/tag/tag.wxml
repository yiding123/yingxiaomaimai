<view class="sub">
  <form bindsubmit="add">
    <view class="item">
      <input class="item-value" name="name" type="text" maxlength="50" placeholder="多个标签用空格分隔" value="{{tag}}"/>
    </view>

    <!-- 提交 -->
    <view class="submit">
      <button class="btn" hover-class="other-button-hover" formType="submit" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" disabled="{{disabled}}">保存</button>
    </view>

  </form>
</view>